{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path'); [[ \"$f\" != *.php ]] && exit 0; if [ -f vendor/bin/phpcs ]; then PHPCS=\"vendor/bin/phpcs\"; elif command -v phpcs &>/dev/null; then PHPCS=\"phpcs\"; else exit 0; fi; \"$PHPCS\" -n --standard=phpcs.xml \"$f\"",
            "timeout": 30,
            "statusMessage": "Verificando PHPCS..."
          }
        ]
      }
    ]
  }
}
